// iteracao arrays
let fruits = ['Banana', 'Laranja', 'Maça', 'Uva'];


let ok = fruits.every(()=>{
    return value.length > 3;
});


includes()// substitui tem que percorrer uma lista para retornar 0/1
filter()//busca
every()// todos satisfatorios
some() // apenas 1 resultado for satisfatorios