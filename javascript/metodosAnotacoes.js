// Metodos de Strings

indexOf(posIni); // se nao encontrar a str, retorna o valor de -1;
slice(posIni, posFim);
replace(strAtual, novaStr);
// Aplicando conceito de CamelCase
toUpperCase(maiusculo);
toLowerCase(minusculo);
trim(removeEspacos,esquerda/direita);
split(sutilArteDeFatiarStrApartirDeumCaractereChave);


// Metodos de Números

toString(); //numero para texto
toFixed(2); // formata para moeda
parseInt(); // transformar em inteiro
parseFloat(); // transformar em float para números decimais
